import styled from "styled-components";

const MainContent = styled.div`
  display: flex;
`;

export default function FAQ() {
  return (
    <MainContent>
      <h2>FAQ</h2>
    </MainContent>
  );
}
