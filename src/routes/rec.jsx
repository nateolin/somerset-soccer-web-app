import styled from "styled-components";

const MainContent = styled.div`
  display: flex;
`;

export default function Rec() {
  return (
    <MainContent>
      <h2>Rec</h2>
    </MainContent>
  );
}
