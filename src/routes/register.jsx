import styled from "styled-components";

const MainContent = styled.div`
  display: flex;
`;

export default function Register() {
  return (
    <MainContent>
      <h2>Register</h2>
    </MainContent>
  );
}
